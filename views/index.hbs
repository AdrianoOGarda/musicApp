<h1>{{title}}</h1>
<p>Welcome to {{title}}</p>

{{#each posts}}
<div>
  <h2>{{picName}}</h2>
  <img src="{{picUrl}}" alt="{{picName}}" style="width:200px;height:200px;object-fit:cover">
  <p>{{content}}</p>
</div>
{{/each}}


<div>
  {{#each musicians}}
  {{>artistCard this}}
  {{/each}}
</div>
